import {deflateSync} from 'node:zlib';
function crc(b){let c=0xffffffff;for(const v of b){c^=v;for(let k=0;k<8;k++)c=(c>>>1)^((c&1)?0xedb88320:0)}return(c^0xffffffff)>>>0}
function chunk(type,data){const t=Buffer.from(type),out=Buffer.alloc(data.length+12);out.writeUInt32BE(data.length);t.copy(out,4);data.copy(out,8);out.writeUInt32BE(crc(Buffer.concat([t,data])),data.length+8);return out}
export function makeIcon(size){const raw=Buffer.alloc((size*3+1)*size);for(let y=0;y<size;y++)for(let x=0;x<size;x++){let count=0;for(let sy=0;sy<3;sy++)for(let sx=0;sx<3;sx++){const a=(x+(sx+.5)/3)/size*2-1,b=(y+(sy+.5)/3)/size*2-1,r=Math.hypot(a,b),theta=Math.atan2(b,a);const arm=(theta+8*r+Math.PI*4)%Math.PI;const on=(r>.18&&r<.68&&arm<.95)||(r>.105&&r<.145);if(on)count++}const t=count/9,i=y*(size*3+1)+1+x*3;raw[i]=Math.round(6+79*t);raw[i+1]=Math.round(16+208*t);raw[i+2]=Math.round(24+184*t)}const header=Buffer.alloc(13);header.writeUInt32BE(size);header.writeUInt32BE(size,4);header[8]=8;header[9]=2;return Buffer.concat([Buffer.from([137,80,78,71,13,10,26,10]),chunk('IHDR',header),chunk('IDAT',deflateSync(raw)),chunk('IEND',Buffer.alloc(0))])}
